# =============================================================================
# Filter-iCal - Nginx Configuration
# =============================================================================
# Generated from projects.yml
# Handles routing for filter-ical.de and www.filter-ical.de
# =============================================================================

# =============================================================================
# PRODUCTION: filter-ical.de
# =============================================================================
server {
    listen 443 ssl http2;
    server_name filter-ical.de www.filter-ical.de;

    # SSL certificates
    ssl_certificate /etc/letsencrypt/live/filter-ical.de/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/filter-ical.de/privkey.pem;

    # Include security headers
    include /etc/nginx/includes/security-headers.conf;

    # Logs
    access_log /var/log/nginx/filter-ical.access.log detailed;
    error_log /var/log/nginx/filter-ical.error.log warn;

    # ==========================================================================
    # BACKEND ROUTES (API)
    # ==========================================================================
    location ~ ^/(api|domains|filter|subscribe|ical|health) {
        # Rate limiting for API
        limit_req zone=api burst=20 nodelay;

        # Proxy to backend
        proxy_pass http://filter-ical-backend;

        # Include standard proxy headers
        include /etc/nginx/includes/proxy-headers.conf;

        # API-specific timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # ==========================================================================
    # FRONTEND ROUTES (SPA)
    # ==========================================================================
    location / {
        # Rate limiting for general traffic
        limit_req zone=general burst=50 nodelay;

        # Proxy to frontend
        proxy_pass http://filter-ical-frontend;

        # Include standard proxy headers
        include /etc/nginx/includes/proxy-headers.conf;

        # SPA fallback for client-side routing
        proxy_intercept_errors on;
        error_page 404 = @frontend_fallback;
    }

    # SPA fallback - serve index.html for client-side routes
    location @frontend_fallback {
        proxy_pass http://filter-ical-frontend/index.html;
        include /etc/nginx/includes/proxy-headers.conf;
    }

    # ==========================================================================
    # STATIC ASSETS (with caching)
    # ==========================================================================
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        proxy_pass http://filter-ical-frontend;
        include /etc/nginx/includes/proxy-headers.conf;

        # Cache static assets
        proxy_cache_valid 200 7d;
        add_header Cache-Control "public, max-age=604800, immutable";
    }
}

# =============================================================================
# STAGING: staging.filter-ical.de
# =============================================================================
server {
    listen 443 ssl http2;
    server_name staging.filter-ical.de;

    # SSL certificates
    ssl_certificate /etc/letsencrypt/live/staging.filter-ical.de/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/staging.filter-ical.de/privkey.pem;

    # Include security headers
    include /etc/nginx/includes/security-headers.conf;

    # Logs
    access_log /var/log/nginx/filter-ical-staging.access.log detailed;
    error_log /var/log/nginx/filter-ical-staging.error.log warn;

    # ==========================================================================
    # BACKEND ROUTES (API)
    # ==========================================================================
    location ~ ^/(api|domains|filter|subscribe|ical|health) {
        # More lenient rate limiting for staging
        limit_req zone=api burst=50 nodelay;

        # Proxy to staging backend
        proxy_pass http://filter-ical-backend-staging;

        # Include standard proxy headers
        include /etc/nginx/includes/proxy-headers.conf;

        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # ==========================================================================
    # FRONTEND ROUTES (SPA)
    # ==========================================================================
    location / {
        limit_req zone=general burst=100 nodelay;

        # Proxy to staging frontend
        proxy_pass http://filter-ical-frontend-staging;

        # Include standard proxy headers
        include /etc/nginx/includes/proxy-headers.conf;

        # SPA fallback
        proxy_intercept_errors on;
        error_page 404 = @frontend_fallback;
    }

    location @frontend_fallback {
        proxy_pass http://filter-ical-frontend-staging/index.html;
        include /etc/nginx/includes/proxy-headers.conf;
    }

    # ==========================================================================
    # STATIC ASSETS
    # ==========================================================================
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        proxy_pass http://filter-ical-frontend-staging;
        include /etc/nginx/includes/proxy-headers.conf;

        # Shorter cache for staging
        proxy_cache_valid 200 1h;
        add_header Cache-Control "public, max-age=3600";
    }
}